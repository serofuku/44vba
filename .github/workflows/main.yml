name: Build 44VBA ESP32-S3
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Fix submodules
        run: |
          git config --global url."https://github.com/".insteadOf "git@github.com:"
          git submodule update --init libretro-common
          git submodule deinit 44gba-watchos || true
          git rm --cached 44gba-watchos || true

      - name: Install ESP-IDF
        run: |
          sudo apt-get install -y git wget flex bison gperf python3 python3-pip python3-venv cmake ninja-build ccache libffi-dev libssl-dev dfu-util libusb-1.0-0
          git clone --depth 1 --branch v5.1.2 --recursive https://github.com/espressif/esp-idf.git ~/esp-idf
          cd ~/esp-idf
          ./install.sh esp32s3

      - name: Patch project files
        run: |
          cd port-esp32s3

          echo "=== Patching sdkconfig.defaults ==="
          grep -v "COMPILER_WARN\|COMPILER_HIDE\|COMPILER_DISABLE\|WERROR\|WARNINGS_AS_ERRORS" \
            sdkconfig.defaults > sdkconfig.defaults.tmp || true
          mv sdkconfig.defaults.tmp sdkconfig.defaults
          echo "CONFIG_COMPILER_DISABLE_GCC12_WARNINGS=y" >> sdkconfig.defaults
          echo "CONFIG_COMPILER_WARNINGS_AS_ERRORS=n" >> sdkconfig.defaults
          echo "CONFIG_COMPILER_WARN_WRITE_STRINGS=n" >> sdkconfig.defaults
          cat sdkconfig.defaults

          echo "=== Patching CMakeLists.txt ==="
          sed -i 's/ -Werror / /g' CMakeLists.txt
          sed -i 's/ -Werror$//g' CMakeLists.txt
          sed -i 's/"-Werror"//g' CMakeLists.txt
          if ! grep -q "Wno-error" CMakeLists.txt; then
            sed -i '/cmake_minimum_required/a add_compile_options(-Wno-error -Wno-unused-but-set-variable -Wno-unused-function -Wno-implicit-fallthrough -Wno-format)' CMakeLists.txt
          fi
          cat CMakeLists.txt

      - name: Build and capture full error
        run: |
          . ~/esp-idf/export.sh
          cd port-esp32s3
          idf.py set-target esp32s3

          export CXXFLAGS="-Wno-error -Wno-unused-but-set-variable -Wno-unused-function -Wno-implicit-fallthrough -Wno-format"
          export CFLAGS="-Wno-error -Wno-unused-but-set-variable -Wno-unused-function -Wno-implicit-fallthrough -Wno-format"

          idf.py build 2>&1 | tee build.log || true

          echo "=== FULL gba.cpp ERROR from ninja stderr log ==="
          # Read the actual ninja error log that contains the real compiler error
          STDERR_LOG=$(find build/log -name "idf_py_stderr_output_*" | sort | tail -1)
          STDOUT_LOG=$(find build/log -name "idf_py_stdout_output_*" | sort | tail -1)

          echo "--- stderr log: $STDERR_LOG ---"
          cat "$STDERR_LOG" || echo "not found"

          echo "--- stdout log: $STDOUT_LOG ---"
          cat "$STDOUT_LOG" || echo "not found"

          echo "=== ninja build log ==="
          cat build/.ninja_log 2>/dev/null | tail -20 || echo "no ninja log"

          echo "=== CMakeFiles error log ==="
          find build -name "CMakeError.log" | while read f; do
            echo "--- $f ---"
            cat "$f"
          done

          exit 1

      - name: Upload all logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs
          path: |
            port-esp32s3/build.log
            port-esp32s3/build/log/
